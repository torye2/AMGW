package amgw.amgw.chat.model;

public class ChatEnums {
    public enum ChatRoomType { DIRECT, GROUP }
    public enum ChatMemberRole { OWNER, MEMBER }
    public enum ChatContentType { TEXT, FILE, SYSTEM }
}